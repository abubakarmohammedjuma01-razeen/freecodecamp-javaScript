// An anonymous function is a funtion without a name that can be assinged to a variable.

const sum = function(sum1, sum2) {
	return (sum1 + sum2);
}

console.log(sum(7, 4));

// The function is stored in a variable and called using that variable:

